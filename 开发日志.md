# NovelForge 开发日志

> 全自动长篇小说生成系统（素材 → 设定 → 大纲 → 写作 → 检查 → 润色 → Word）
> 操作手册见 AGENTS.md；架构文档：`E:\AI\Hermes_Workspace\output\NovelForge_架构设计与可行性评估_v2.md`

---

## GUI 化总路线（2026-09-09 定调；来源：AI-Novel-Writer 形态评估）

**定调**：AI-Novel-Writer（EthanYoQ，GPL-3.0，719 star，v1.1.0）的本地优先桌面形态 = 墨坊最高理想形态，GUI 化从可选项升级为目标形态。评估全文：skill ai-novel-pipeline `references/ai-novel-writer-lessons.md`。**GPL 红线：禁 fork/禁复制其代码，只借产品范式**。

- **P0 · Python 本地 API 服务**（本次开工）：把 orchestrator/stages/approve/reject 包成本地 HTTP 服务（stdlib http.server 零依赖，tegula 同模式）。端点：`POST /stage/{n}/run`（job_id）/ `GET /jobs/{id}` / `GET /state`（进度+门+成本）/ `POST /approve` / `POST /reject` / `POST /refine` / `GET /models`（engine/model 配置回显）。**验收硬标准：不用 Hermes，纯 curl 跑通 stage1→2 全链 + 审批门**
- **P1 · Electron 桌面控制台**（3-5 天首版）：worldbuilding-desktop-tools 先例（Electron+Vue）；信息架构=项目树（书/章）→ 流水线视图（七阶段/门状态）→ 审稿收件箱（审批门 2/6 + refine 意见）→ 成本面板（cost_log 图表+预算余量）→ 设置（engine/model/providers）；ROSA 雾灰紫 tokens
- **P2 · 专属应用承接订阅**：Electron 壳内直连订阅 SDK（llm_client 加一个 provider 即可）；插件化分发仅当有外部用户再评估
- 抄范式清单：单次运行模型冻结 / Proposal 收件箱（逐条采纳才改权威状态）/ 不可变快照+确认清单修订 / 上下文注入可视化（成本信任靠可见性）

---

### P2.1 TokenHub 直连 E2E（2026-09-09 当日晚，stage1 三轮跑通）

- **引擎切换**：providers.tokenhub base_url=tokenhub.tencentmaas.com/v1（**与 Hermes config 同源域名，勿用文档的 tencentcloudmaas.com——key 与域名绑定，错域 401**）；三角色默认 deepseek-v4-flash（¥1/¥2 每百万，TokenHub 模型价格 2026-09-08）；RATES 补 TokenHub 广州价目
- **KEY 位置**：`C:/Users/muyu/AppData/Local/hermes/.env` 的 TOKENHUB_API_KEY（脚本复制到项目 .env，全程脱敏）；本机其他位置（profiles/Windows env/shell）均无
- **E2E 实战（stage1，14 卡归并）**：第1轮暴露三 bug（内联输入被当输出→拆 30 请求；路径带引号未剥；目录正则不匹配「xx/ 下的 *.md（说明）」带空格+括号形态）→ 修复后第2轮 0.060 元跑通但占位符设定集+根目录散落文件 → 二次修复（期望输出排除内联区/缺失警告区/括号内路径/噪声字符，同 basename 去重保留 data/ 前缀）→ 第3轮 ¥0.031 **单请求完美收官**：setting.json 14.6K（9 角色/12 碎片/17 时间线/0 占位）、根目录零残留
- **教训**：①连通性无需自测——Hermes 内部天天在调，key 是活的；域名/接法要从 Hermes config 照抄而不是从文档猜；②mock 全绿 ≠ 真实模型行为，同 basename 重复路径/引号路径/括号噪声都是真模型教出来的；③**模型白名单纪律（用户定调）：免费体验包按模型领取，未经用户确认不得指定付费模型**（本轮 v4-flash 三笔 ~0.1 元是否落包待用户确认）；④run_task 无输出时 pipe tail 会吃掉 exit code，验结果以磁盘产物+cost_log 为准
- 提交：26f31db（TokenHub 切换）→ 9a38cab（解析修复）
- 待办：用户确认赠包模型清单 → 必要时一行切角色路由；--stage 2 大纲 E2E（用户指令后）

## GUI 化 P0：本地 API 服务 nf_api（2026-09-09）

**背景**：AI-Novel-Writer（GPL-3.0，719 star）桌面形态=墨坊目标形态（总路线见日志顶部「GUI 化总路线」）。方寸任务 task-20260909-002。P0 先行：把流水线内核包成本地 HTTP 服务，为 P1 Electron 控制台供数。

### 交付物

1. `scripts/nf_api.py`（新）：stdlib http.server 零依赖，默认 127.0.0.1:8765
   - 端点：GET /health /state（progress+gates+成本汇总+最近job）/models（engine/providers/model 回显）/jobs/{id}；POST /stage/{n}/run（202+job_id，后台线程执行）/approve /reject /refine/outline /refine/chapter /snapshot
   - 函数级复用：orchestrator.run / ProgressManager.set_approved / reject.reject_stage / refine_outline.run_refine / refine_chapter.run_refine——不经 Hermes 子进程、不重复造轮子
   - 单任务互斥（409）、审批门 exit=3 映射为 job ok（"等待审批"是正常门暂停，非失败）
   - `_client_for_env`：NF_API_ALLOW_FAKE=1 时注入 utils/fake_client.FakeClient（无 LLM 全链测试），生产模式走 make_client 真引擎
2. `scripts/utils/fake_client.py`（新）：与真客户端同签名的假执行器，按任务名写出通过各阶段确定性校验的产物（setting.json 四键/global.md 起承转合/逐章大纲三字段/章节正文达标字数/checked/refined 复制链）
3. `scripts/nf_api_selftest.py`（新）：21 用例验收自测（起服务→纯 HTTP 驱动）。⚠️ 运行会清空 data/ 与 logs/（当前书档产物销毁，history/ 快照保留）
4. stage1_consolidate 补 cost 记账（P0 起漏记 stage1 流水，本属"stage2-6 全记账"漏洞）
5. AGENTS.md：命令速查加 nf_api/自测条目；硬性约束加本地 API 安全边界

### 验证

- py_compile PASS；nf_api_selftest **21 PASS / 0 FAIL**
- 覆盖：health/models/state、stage1→2 fake 全链（产物落盘）、审批门（stage2 done+approved=false → 未审批重跑=门暂停 exit3）、approve/revoke/未完成审批 409、reject dry-run 不清产物、refine dry-run、非法阶段 400、409 互斥、404
- **P0 验收硬标准达成：纯 curl/HTTP 可跑通 stage1→2 全链 + 审批门，全程不依赖 Hermes**

### 陷阱

- **client=None 的两层坑**：调用点内部 `client or make_client(...)` 会在缺 key 时 SystemExit（且压过 FakeClient 注入）→ 服务层必须显式构造客户端传入；orchestrator.run(client=...) 透传链路已验证
- HTTPError 在 urllib 不自动解析 body → 测试 req() 辅助要接 4xx/5xx 响应体，否则 404/400 用例直接抛异常
- cost_log 只见 stage2 流水 ≠ stage1 没跑，是 stage1 一直没记账（本次补）

### 待办（P1 前）

- E2E 真实 LLM（等 HUNYUAN_API_KEY）→ nf_api 生产模式跑 stage1→2
- P1 Electron 控制台：信息架构见日志顶部总路线

## P2 引擎无关化：OpenAI 兼容直连 + 腾讯混元接入（2026-09-09）

**定调背景**：Hermes 仅为过渡执行引擎（token 订阅可能仅限专属应用），写作成本改打腾讯云免费额度渠道。方寸任务 task-20260909-001。

### 交付物

1. `scripts/utils/llm_client.py`（新，~480 行）：LLM 执行层抽象
   - `make_client(cfg, role)` 工厂：按 system.yaml 的 engine/model.*/providers 选择引擎；engine=hermes 时返回内置 HermesClient（原 api_client 逻辑保留），engine=direct 时返回 OpenAICompatClient
   - OpenAICompatClient：urllib 零新依赖；输入内联（解析「输入文件」段 → 全文内联进单请求）；输出协议 `===FILE/APPEND/DELETE: 路径=== ... ===END===`；按文件名贴齐期望路径 + 白名单（data/** 与 logs/runs.db）；多文件输出任务按目标文件拆分请求；usage 取 API 真实返回；write_task 与 HermesClient 同构
   - `.env` 加载器（_load_env_file，不覆盖已有环境变量）
   - **源码零反斜杠字面量纪律**（BS=chr(92) 动态构造）：工具链 JSON 参数会把 \\ 半化成 \，字面量反斜杠在 write_file 时会炸出 unicodeescape SyntaxError
2. `config/system.yaml` 新 schema：engine/providers/model.{default,writer,checker}.{provider,id}；当前默认 default/checker=hunyuan-lite（免费）、writer=hunyuan-a13b（刊例 in 0.5/out 2 元每百万，官方计费文档 2026-06 核实）；混元 OpenAI 兼容端点 api.hunyuan.cloud.tencent.com/v1；免费包 100 万 tokens 优先扣、耗尽默认不自动转后付费
3. `scripts/utils/cost_tracker.py` 重写：RATES 增 hunyuan 两档（lite 免费）；新增角色化计价 resolve_rate（hermes 引擎按角色 default/writer/checker 计价，直连按真实 model_id）；CostTracker.charge_cost(result)（从 run_task 返回 dict 取 model/provider/role 记账）；record 旧签名兼容
4. 11 个调用点全部接 make_client（7 阶段 + book_summary/refine_outline/refine_chapter/setting_refine/rosa_postprocess），5 处 cost.record 改 charge_cost，stage4 估算补模型元数据；orchestrator 补 BUDGET_LIMIT_YUAN 环境覆盖
5. `scripts/llm_client_selftest.py`（新，29 用例 mock 集成测试，不联网）：路径解析/内联/ops 解析（围栏剥离、双反斜杠归一、同行标记）/期望输出提取/拆分/白名单/贴齐/mock run_task 全链；**改 llm_client 解析逻辑必须重跑此脚本全绿**
6. .env.example（HUNYUAN_API_KEY 等）；AGENTS.md 硬性约束/README 架构概览同步引擎可切换语义

### 验证

- py_compile 全量 PASS；orchestrator 全模块 import PASS
- llm_client_selftest 29 PASS / 0 FAIL
- make_client 工厂冒烟：direct（缺 key 构造成功调用报错）/ hermes 回退 / 旧配置字符串模型名回退 hermes / write_task 同构
- 真实确定性链路：stage1_consolidate CLI 对《留下你的歌》14 卡归一化 + 指纹 manifest 落盘成功
- E2E 真实 LLM 调用：待用户提供 HUNYUAN_API_KEY 后跑（见下方恢复步骤）

### 陷阱（重要）

- **write_file JSON 参数反斜杠半化**：含 \\ 的 Python 源码经 write_file 落盘会变成 \，触发 unicodeescape SyntaxError → 解析类模块用 BS=chr(92) 动态构造正则（本文件先例）；或先落盘再 patch 修
- **旧 make_client 硬崩分支残留**：patch 重写时新旧两版并存导致直连模式构造即 SystemExit（dry-run 也挂）→ 保持"构造成功、调用时才验证凭据"
- **patch 单参数补块会误删相邻行**：_post_chat 补 _validate_creds 时丢了 payload 构造行 → 补块后必须跑 py_compile + mock 测试确认
- **路径正则过严**：只认盘符绝对路径会漏相对路径（Temp\ 等），统一定义为"可选盘符 + 路径体 + 扩展名"；白名单用 Path.resolve + parents 判断，勿用字符串子串
- mock 测试断言值要与 mock 数据一致（tokens 求和 100+100、tokens_out 50+50），错值会浪费一轮排查

### 恢复步骤（E2E：等用户 key）

1. `E:/CODE/CangKu/NovelForge/.env` 写 `HUNYUAN_API_KEY=<用户的key>`（key 本身永不入对话/git/日志）
2. 试跑 `python scripts/utils/llm_client.py`（不存在入口则用）：先用临时任务文件跑一次单请求冒烟（读 .env → chat → 输出协议）
3. 真实 E2E：`python scripts/orchestrator.py --stage 1`（LLM 归并 14 卡 → setting.json，hunyuan-lite 免费额度内）→ 校验通过后 `--stage 2` 看大纲质量再审批
4. 若 lite 质量不够，writer 换 hunyuan-a13b（免费包优先扣）、checker 同理；再不够才考虑付费档

## 2026-08-13 开发记录（P0.5：大纲精修模式 + 用户大纲输入位）

### 目标

针对审阅反馈的三项缺口：大纲审批前无体检辅助（易草草开工）、审批门无迭代通道（打回=全量重跑）、系统不接收用户自带大纲。全部为非破坏性新增。

---

### 1. 大纲体检报告（确定性脚本）✅

**新增文件**：`scripts/outline_review.py`

**问题**：阶段 2 校验只查"起承转合四节存在 + 关键节点≥1 + 预计章节数>0"，空泛大纲也能过审；用户拿到整篇 global.md 无薄弱点提示。

**实现**：
- 解析 `data/outline/global.md` 的关键节点条目与章节规划条目
- 每条按 5 维打分：篇幅（去空白 ≥60 字）/ 具体事件动词 / 冲突张力词 / 角色名（从 setting.json 提取）/ 场景锚点（地点名 + 场景后缀词表）
- 判定：≥4 OK / =3 WARN / ≤2 THIN；整体一致性检查（节点数 vs 预计章节数、规划条数 vs 预计章节数）
- 输出终端摘要 + 详细报告 `data/outline/review_report.md`

**验证**：现有 3 章 global.md → 9 条目（6 节点 + 3 规划），8 OK / 1 THIN（节点1 群像概述确为最薄条目，判定合理）。

**踩坑**：条目提取正则初始带 `re.S` 导致第一条吞掉后续所有条目（只解析出 1 条）；`第 1 章` 数字间有空格，正则需 `第\s*[\d一二三四五六七八九十百]+\s*章`。

---

### 2. 大纲精修模式（增量修订 + 版本留痕）✅

**新增文件**：`prompts/stage2_refine.md`、`scripts/refine_outline.py`

**问题**：审批门只有"批准 / 打回全量重跑"二选一，用户定向意见（如"第3章侧重露汐"）无法增量落地，返工烧 token 且丢已确认结构。

**实现**：
- 精修任务文件自包含：嵌入当前 global.md 全文 + 用户意见 + 设定集路径，子会话按"增量修订"原则（保留已确认结构，只改意见涉及部分）
- `refine_outline.py "意见"`：读大纲 → 备份 `data/outline/history/global_vN.md`（shutil.copy2 保留换行）→ 生成任务 → 子会话修订 → `check_global_outline` 结构校验 → 体检摘要输出
- `--dry-run`：只备份 + 生成任务文件，不调用子会话（零成本预演）
- 精修不改变审批状态（approve 仍需用户显式执行）

**验证**：dry-run 全链路 13 项 PASS（YAML 解析 / 占位符替换 / 任务文件嵌入 / 备份生成 / stage2 回归）。

**坑位提示**：`current_outline` 占位符必须最后替换（模板加载器按 key 循环 replace，大纲内容若含 `{{...}}` 会被后续替换误伤）。

---

### 3. 用户大纲输入位 ✅

**修改**：`config/project.yaml`（新增 `book.user_outline`）、`prompts/stage2_global_outline.md`（新增"用户提供的大纲/构思，最高优先级"节）、`scripts/stage2_outline.py`（build_task 传参）

**实现**：用户提供梗概/大纲/核心构思后，stage2 生成整体大纲时优先遵循；留空则模板显示"（未提供，请基于设定集与素材清单自拟）"，行为与原来一致。

---

### 验证汇总

`Temp/hermes-verify-p05.py` 13/13 PASS → 已清理临时脚本与 dry-run 痕迹（history/global_v1.md、stage2_refine.md 任务文件）。

### 后续（未实施，列入 P1/P2）

- stage1.5 设定完备度评估 + `llm_inferred` 标记补全（素材碎场景根治）
- stage1 素材更新 bug：`stage1_consolidate.py` 在"设定集已存在且有效"时跳过归并，新素材不会进入 setting.json（与 AGENTS.md 手册描述矛盾）
- gate/reject_stage 文件机制（打回流程正规化）

---

## 2026-08-13 开发记录（P0.6：Word 模板接入 + 成本记账修复 + 快照机制）

### 目标

三项：① 用户 Word 模板接入（`小说格式模板V1.3.dotx` 底版，预留换模板入口）；② 成本记账全面修复（此前仅 stage4 记账且用假数兜底，数据不可信）；③ 补上 history/ 快照机制（此前为空洞承诺，data/ 不进 git 却无任何版本备份）。

---

### 1. Word 模板接入 ✅

**新增**：`templates/小说格式模板V1.3.dotx`（用户提供，进 git）；`config/project.yaml` 的 `book.word_template` + `book.author`；`config/system.yaml` 的 `chapter.page_break`

**改造**：`scripts/stage7_convert.py` 重写
- `.dotx` 归一：python-docx 不认 `template.main+xml` content type，`dotx_to_docx()` 用 zipfile 替换 `[Content_Types].xml` 后缓存到 `data/tmp/template_cache/`
- 模板底版启动：`Document(template)` 替代空文档，保留模板的样式/页眉页脚/页面设置（A4、2.5cm 边距）
- 占位符替换：`[书籍标题]`→书名（22pt 黑体样式）、`[作者]`→author（14pt 宋体样式）、`[目录占位符]`→TOC 域（Word 打开 F9 更新）、`[请输入文本]`→正文锚点（正文段落插入其前）
- 正文 `## 第N章` → Heading 1 + 每章新起一页（page_break 可关）；`# 书名` 跳过（模板已有书名页）
- 无模板时回退 P0 硬编码路径（向后兼容）
- **换模板入口**：改 `book.word_template` 或覆盖 `templates/` 下文件，无需改代码

**验证**：模板底版输出 206 段、书名/作者/锚点替换正确、TOC 域存在、3 个 Heading 1 均分页；无模板回退正常。

---

### 2. 成本记账修复 ✅

**问题**：此前只有 stage4 记账，且 `result["tokens"] or 20000` 假数兜底；stage2/3/5/6 的 LLM 费用完全不记 → 预算熔断形同虚设。

**改造**：
- `utils/api_client.py`：`parse_usage()` 增强解析（tokens_in/prompt_tokens/total_tokens 等）；解析不到时 `estimate_tokens()` 按任务文件大小保守估算（输入≈文件字节×0.4，输出≈输入/3），返回 `estimated=True` 标记，杜绝假 0
- `utils/db.py`：cost_log 加 `estimated` 列 + 幂等迁移（`_migrate()` ALTER TABLE）
- `utils/cost_tracker.py`：`record(..., estimated=)` 透传
- stage2/3/4/5/6 全部接入 `cost.record`（失败也记，token 已消耗）；stage4 去掉假数兜底

**验证**：旧库自动补列、estimated 正确落库、parse_usage 两种路径、估算非零。

---

### 3. 项目快照机制 ✅

**新增**：`scripts/snapshot.py`
- 复制关键产物（progress.json/setting.json/outline/chapters/rolling.md/book.md）到 `history/{时间戳}_{标签}/`
- 保留最近 10 份（KEEP 可配），超出删最旧；CLI `--list` 查看
- orchestrator 每阶段成功后自动 `snapshot(f"stage{n}_done")`（失败不阻塞流程）

**验证**：快照创建（7 项复制，152KB）、关键文件存在、保留策略正确、CLI 可用。

---

### 验证汇总

ad-hoc 验证（tempfile 路径 + hermes-verify- 前缀）28/28 PASS：config 新字段 / dotx 转换 / 模板底版输出（占位符+TOC+分页）/ 无模板回退 / 记账迁移与落库 / parse_usage 两路径 / snapshot 创建与保留 / 11 脚本 py_compile / 6 阶段记账接入。验证脚本与临时产物已清理（data/tmp/template_cache/ 缓存保留，stage7 复用）。

### 遗留（未实施）

- 模型路由仍是死配置（`model.writer/checker` 与模板 frontmatter 的 model 未传给 `hermes chat -q`）
- 30 万字分卷输出、章节级精修复用、多书隔离（见 AGENTS.md 讨论）

---

## 2026-08-13 开发记录（P0.7：素材链路修复 + 干预能力 + 写作质量 + 多书隔离）

### 目标

按功能清单批量落地：① 素材更新 bug；② vault_links 索引；③ 章节级精修；④ 模型路由；⑤ 风格模仿；⑥ 全书摘要；⑦ 润色后体检；⑧ 成本报告；⑨ 多书目录隔离。用户指示：章节摘要不做（改为完书后全书摘要，供填充 ROSA 粘贴），其余全做，多书隔离优先。

---

### 1. 素材更新 bug（stage1 增量归并）✅

**问题**：`stage1_consolidate.py` 在"设定集已存在且有效"时无条件复用，新增素材永远进不了 setting.json（与 AGENTS.md 手册矛盾）。

**修复**：`manifest_fingerprint()`（归一化文件内容 sha256，与 mtime 无关）→ manifest 落盘带 `_fingerprint` → 素材实质变化才触发重归并子会话。验证：同清单同签名/内容变签名变/仅 mtime 变签名不变。

### 2. vault_links 设定库引用索引 ✅

**新增**：`scripts/build_vault_links.py`（此前 project.yaml 声明但从未实现）。从 setting.json 聚合 source 字段 → `materials/vault_links.md`（素材→角色/世界观/情节碎片/时间线条目分组 + 统计 + 未溯源清单）。stage1 归并成功后自动调用。验证：55 条目 / 6 素材。

### 3. 章节级精修 ✅

**新增**：`scripts/refine_chapter.py` + `prompts/stage4_refine.md`。复用大纲精修模式：备份 `data/chapters/history/chNN_vM.md` → 子会话增量修订（保留事件顺序/对话/伏笔，±20% 字数铁律，quality 重评）→ 校验。CLI：`refine_chapter.py 3 "意见"`。踩坑：run_refine 漏 `client = client or HermesClient()` 初始化（与 refine_outline 不一致），dry-run 即暴露，已修。

### 4. 模型路由 ✅

**问题**：`model.writer/checker` 与模板 frontmatter 的 model 全是死配置，api_client 不读。

**修复**：`hermes chat` 支持 `-m/--model`（已确认）。HermesClient 加 `model` 参数，run_task 拼接 `-m`；stage1/2/3/refine×2 → default、stage4/6 → writer、stage5 → checker。验证：mock subprocess 捕获 cmd 含/不含 -m。

### 5. 风格模仿 ✅

`config/project.yaml` 加 `book.style_reference`（可选路径）→ stage4 任务注入范文（上限 6K 字符，自包含）→ 模板加"风格参考"节。踩坑：中文字符串内嵌全角引号被判语法错误，改写避引号。

### 6. 全书摘要（完书后）✅

**新增**：`scripts/book_summary.py` + `prompts/book_summary.md`。收集各章 `<!-- summary -->` 注释 → 子会话聚合 600-900 字全书摘要（一句话概括/世界观/主线/角色弧光/结局）→ `output/{书名}_全书摘要.md`，供填充 ROSA 粘贴。

### 7. 润色后体检 ✅

**新增**：`scripts/polish_review.py`（确定性）。汇总 refined 章：字数/quality/与 checked 变化，标出低质量(<6)/超±20%/缺 quality 章 → `data/outline/polish_review.md`。交付 Word 前跑。

### 8. 成本报告 CLI ✅

**新增**：`scripts/cost_report.py`。总览（总额/估算占比/按阶段表）+ `--by-chapter`（stage4 分章）+ `--runs N`。踩坑：真实 logs/runs.db 是旧库无 estimated 列，直连查询报错 → _conn() 加幂等迁移（与 RunDB._migrate 一致）。

### 9. 多书目录隔离 ✅（用户重点）

**新增**：`scripts/switch_book.py`（归档式切换，不动现有路径约定）：`data/books/{书名}/` 归档/恢复，切书 = 归档当前 + 恢复目标，数据永在 data/ 或 data/books/ 中；`--list` 查看；归档/恢复均需 `--yes`（保护内置在函数而非仅 CLI）。orchestrator 启动时书名不一致提示（不阻断）。

---

### 验证汇总

ad-hoc 验证（项目 Temp/ + runpy 执行，规避 lifecycle_guard Windows 路径 bug）：
- 全量 21 脚本编译 PASS；模型路由 mock 捕获 cmd；风格注入两态；book_summary 3 章收集 + dry-run；polish_review 3 章 quality 8.0；cost_report 真实库总览 0.132 元；switch_book 模拟目录 12 项（归档/恢复/幂等/yes 门）全过；成本报告迁移 bug 修复后复验通过。
- 临时脚本与模拟目录全部清理，工作区干净。

### 遗留（P2+）

- stage1.5 设定完备度评估 + llm_inferred 补全（素材碎根治，需用户确认补全内容）
- 质量自评分重写闭环（quality<6 自动轻量重写）
- gate/reject_stage 文件机制（打回正规化）
- 30 万字分卷输出、设定集跨书复用、多平台导出

---

## 2026-08-14 开发记录（P1.5 素材锤炼 + P1.6 ROSA 后处理）

### 目标

按用户审阅通过的增强方案 v0.2 落地两项：① 素材初期锤炼（完备度体检 + 设定补全门，治"素材碎/人物设定连带完善"）；② 完书后 ROSA 后处理（作品介绍页/角色出场记录/新角色设定草稿，免手工填 ROSA 词条）。用户决策：出场分级按"首次章序+戏份"、补全门挂审批门前强制提醒、新角色草稿允许"待补充"占位。

### 1. 素材完备度体检（确定性，零成本）✅

**新增**：`scripts/material_review.py`
- 对 setting.json 每个角色 6 维打分（身份/性格/能力/关系网/素材覆盖/溯源），≥4 OK / =3 WARN / ≤2 THIN（碎片）
- world/plot_fragments/timeline 完备性三档判定
- 输出 `data/setting/material_review.md` + 终端摘要；`--json` 机器可读
- orchestrator stage1 成功后自动调用（失败不阻断，同 vault_links 模式）

### 2. 设定补全门 ✅

**新增**：`scripts/setting_refine.py` + `prompts/stage1_refine.md`
- 复用 refine_outline 模式：备份 `data/setting/history/setting_vN.json` → 自包含任务文件（嵌入当前设定全文 + 体检 THIN 清单）→ 子会话增量补全
- 硬约束：仅从素材推断、新增字段带 `llm_inferred: true`、locked 条目不动、不删除既有条目
- 补全后校验 + 差异摘要 + 体检复评
- **审批门集成**：orchestrator 在阶段2审批暂停前检查 THIN 并提醒先补全（`gates.setting_refine_reminder` 可关）
- 踩坑：`--roles` argparse 与 store_true 同名冲突 → 改名 `--role-records`；absent 分支漏 `level` 键 → KeyError

### 3. 角色出场统计（确定性核心）✅

**新增**：`scripts/appearances.py`
- 角色集 = 设定集 ∪ 逐章大纲"涉及角色"（正则解析 id+中文名，过滤括号描述）
- 统计 first/chapters/total/per_chapter，分级按"首次章序+戏份"启发式（短篇 3 章须 ≥3 章全勤才 major，避免配角误升）
- 输出 `data/state/appearances.json`；与第1章正文人工核对 3 角色计数 100% 一致
- 踩坑：ROLE_ITEM_RE 正则过宽把括号描述当角色名（"叙事视角）"碎片）→ 重写为"可选 id + 中文名 + lookahead（"；群体名词（走私团伙等）加 GROUP_NOUNS 过滤

### 4. ROSA 后处理 CLI ✅

**新增**：`scripts/rosa_postprocess.py` + `prompts/rosa_book_page.md` + `prompts/rosa_role_page.md` + `config/templates_rosa.yaml`
- 三件草稿 → 沙盒 `E:/图书馆/ROSA/Obsidian_AI_Sandbox/10_Inbox/`（ROSA 只读红线不变，人工审阅后发布）：
  - 作品介绍页（frontmatter/概述/登场角色三组/目录/主要剧情，对齐官方书籍模板 2026-1式）
  - 已有词条角色 → "官作出场记录"待粘贴段落（露汐词条同款格式）
  - 无词条新角色 → 设定页草稿（官方角色模板 2026-2式，源稀薄处"待补充"不杜撰）
- 选项：`--books/--role-records/--all`（默认）/ `--dry-run`（零成本预演）/ `--no-llm`（确定性骨架，不调 LLM）/ `--roles "露汐,小林"`（精确选择）
- 模板路径走 config/templates_rosa.yaml，改路径不改码

### 验证汇总

- 全量 23 脚本 py_compile PASS；material_review THIN/WARN/OK 判定 + world 三档（临时目录 3 角色用例）
- setting_refine dry-run（备份/任务文件/占位符替换 0 残留）
- appearances 真实数据：12→8 角色（过滤群体名词与括号碎片），3 角色计数对照正文 100% 一致
- rosa_postprocess：dry-run / no-llm（9→5 文件）/ LLM 真实子会话（作品介绍页 70 行，结构完整）/ --roles 精确选择 2 文件
- ROSA 只读校验：库本体零写入（仅沙盒 10_Inbox 新增草稿）
- 临时脚本与测试产物已清理（沙盒草稿测试文件删除）

### 遗留（P2+）

- 质量自评分重写闭环（quality<6 自动轻量重写）
- gate/reject_stage 文件机制（打回正规化）
- 30 万字分卷输出、设定集跨书复用、多平台导出
- 出场记录 stage4 实时增量（P1.6 方案 2.6 节，当前完书后一次性统计已覆盖）
- AGENTS.md 命令速查表已补（2026-08-14 提交 0c211aa）

---

## GUI 化 P2 对标补完：项目结构 + AI 助手 + 批量控制（2026-09-12）

**对标基准**：AI-Novel-Writer v1.1.0（EthanYoQ/GPL-3.0/755★）的三栏形态（项目树+工作区+AI 助手）。已完成 Phase A+B（流水线/素材/设定/收件箱/Story Bible/分章），当前缺：项目结构树总览、AI 助手侧栏、批量任务控制、故事弧视图、导出面板。

### 已完成（本批次）

| 项 | 说明 |
|----|------|
| 项目结构树 | 左侧导航树显示全书资产（配置/架构/大纲/章节/草稿）+ 状态标记（已完成/待生成/N/M） |
| 小说配置页 | 独立 UI 配置书名/类型/目标受众/结构/视角/章数/每章字数（告别 yaml） |
| AI 助手侧栏 | 右侧聊天面板：深度思考选择 / 模型选择 / 工作流命令（`/蓝图` `/角色`等） |
| 批量任务控制 | 阶段4/6 批量运行时支持暂停/取消/恢复（通过 `/stop` + 断点） |
| 故事弧视图 | 主线+支线进度条，点击跳转到对应章节 |
| 导出面板 | 分卷 Markdown 导出（独立目录）+ 最终 DOCX |
| 导入参考资料 | 素材管理加「知识库」类型，写作时可引用 |

### 关键对照：我们 vs AI-Novel-Writer

| 能力 | AI-Novel-Writer | NovelForge 当前 | 差距 |
|------|-----------------|-----------------|------|
| 项目树状态总览 | ✅ 左栏 | ✅ 已完成 | — |
| AI 助手侧栏 | ✅ 聊天 | ✅ 已完成 | — |
| 章节批量控制 | ✅ 暂停/取消 | ✅ 已完成 | — |
| 故事弧视图 | ✅ 主线+支线 | ✅ 已完成 | — |
| 知识库导入 | ✅ RAG+FTS | ⚠️ 仅素材引用 | 需 embedding |
| 角色关系图 | ✅ 视觉图谱 | ⚠️ Story Bible 表格 | 需可视化 |
| 审稿→修稿 | ✅ 逐项+证据 | ✅ 报告+精修 | — |
| 来源标注 | ✅ v1.1.0 | ❌ 无 | 低优 |

### 下一步（用户确认后推进）

| 优先级 | 项 | 工作量 | 价值 |
|--------|-----|--------|------|
| **P2.1** | 角色关系可视化（节点图，基于 setting.json 角色关系字段） | 1 天 | 直观呈现角色网络 |
| **P2.2** | 章节蓝图编辑器（每章独立：核心事件/涉及角色/功能/伏笔） | 1.5 天 | 写作前精细化规划 |
| **P2.3** | 质量自评分重写闭环（quality<6 自动触发轻量重写，不烧全章） | 0.5 天 | 减少人工干预 |
| **P2.4** | 知识库 embedding 接入（导入参考文本，写作时 RAG 检索注入） | 1 天 | 跨书复用+正典一致性 |
| **P3** | 多平台发布（Markdown 分卷/订阅 SDK） | 2 天 | 商业化前置 |

---

## GUI 化 P2 对标补完：项目结构 + AI 助手 + 批量控制（2026-09-12）

**对标基准**：AI-Novel-Writer v1.1.0（EthanYoQ/GPL-3.0/755★）的三栏形态（项目树+工作区+AI 助手）。已完成 Phase A+B（流水线/素材/设定/收件箱/Story Bible/分章），当前缺：项目结构树总览、AI 助手侧栏、批量任务控制、故事弧视图、导出面板。

### 已完成（本批次）

| 项 | 说明 |
|----|------|
| 项目结构树 | 左侧导航树显示全书资产（配置/架构/大纲/章节/草稿）+ 状态标记（已完成/待生成/N/M） |
| 小说配置页 | 独立 UI 配置书名/类型/目标受众/结构/视角/章数/每章字数（告别 yaml） |
| AI 助手侧栏 | 右侧聊天面板：深度思考选择 / 模型选择 / 工作流命令（`/蓝图` `/角色`等） |
| 批量任务控制 | 阶段4/6 批量运行时支持暂停/取消/恢复（通过 `/stop` + 断点） |
| 故事弧视图 | 主线+支线进度条，点击跳转到对应章节 |
| 导出面板 | 分卷 Markdown 导出（独立目录）+ 最终 DOCX |
| 导入参考资料 | 素材管理加「知识库」类型，写作时可引用 |

### 关键对照：我们 vs AI-Novel-Writer

| 能力 | AI-Novel-Writer | NovelForge 当前 | 差距 |
|------|-----------------|-----------------|------|
| 项目树状态总览 | ✅ 左栏 | ✅ 已完成 | — |
| AI 助手侧栏 | ✅ 聊天 | ✅ 已完成 | — |
| 章节批量控制 | ✅ 暂停/取消 | ✅ 已完成 | — |
| 故事弧视图 | ✅ 主线+支线 | ✅ 已完成 | — |
| 知识库导入 | ✅ RAG+FTS | ⚠️ 仅素材引用 | 需 embedding |
| 角色关系图 | ✅ 视觉图谱 | ⚠️ Story Bible 表格 | 需可视化 |
| 审稿→修稿 | ✅ 逐项+证据 | ✅ 报告+精修 | — |
| 来源标注 | ✅ v1.1.0 | ❌ 无 | 低优 |

### 下一步（用户确认后推进）

| 优先级 | 项 | 工作量 | 价值 |
|--------|-----|--------|------|
| **P2.1** | 角色关系可视化（节点图，基于 setting.json 角色关系字段） | 1 天 | 直观呈现角色网络 |
| **P2.2** | 章节蓝图编辑器（每章独立：核心事件/涉及角色/功能/伏笔） | 1.5 天 | 写作前精细化规划 |
| **P2.3** | 质量自评分重写闭环（quality<6 自动触发轻量重写，不烧全章） | 0.5 天 | 减少人工干预 |
| **P2.4** | 知识库 embedding 接入（导入参考文本，写作时 RAG 检索注入） | 1 天 | 跨书复用+正典一致性 |
| **P3** | 多平台发布（Markdown 分卷/订阅 SDK） | 2 天 | 商业化前置 |

---

## 2026-08-14 开发记录（P1：reject 门 + 润色审阅门）

### 目标

补齐干预链路两个环节：① 打回/重跑正规化（此前手动清产物+--from N，原因无记录、残留易漏删）；② 润色稿审阅门（stage6 完成后自动进 stage7，用户无法先审润色稿）。

### 1. reject 门（打回正规化）✅

**新增**：`scripts/reject.py`
- `--stage N [--reason "原因"] [--dry-run]`：记录打回原因（progress stages[N].rejected + rejected_at）→ 按 DOWNSTREAM_ARTIFACTS 映射清理阶段 N 及下游产物（history/ 备份保留）→ 重置下游为 pending → **撤销阶段 N 的审批**（否则重跑完成后审批门不重新拦截）
- 产物映射：打回2=逐章大纲+全部章节+合并+Word；打回3=章节+合并+Word；打回4/5/6/7 逐级收缩；global.md 与 setting.json 不删（走精修通道）
- orchestrator 集成：阶段开始前若 stages[N].rejected 存在 → 打印打回原因提示；阶段重跑成功后清除标记

### 2. 润色稿审阅门 ✅

**修改**：`config/system.yaml` 的 `gates.require_approval: [2, 6]`；`scripts/orchestrator.py` 审批门从硬编码 stage2 泛化为遍历 require_approval 列表
- stage6 完成后暂停：提示审阅 data/chapters/refined/，满意 `approve.py --stage 6` 放行，不满意 `reject.py --stage 6 "意见"` 打回重跑
- stage2 提示保留原样；P1.5 设定补全提醒仅挂在 stage2 审批前（2 in pending_approvals）

### 验证汇总

- 真实数据：stage6 done+未approved → `--stage 7` 触发润色审阅门暂停（return 3）；`approve --stage 6` 后放行完成
- reject 临时目录用例（dry-run 展示 / 真实打回状态与产物 / 非法阶段拒绝 / approved 撤销 / 重跑后审批门重新生效）全过
- orchestrator 打回标记：注入 rejected → 运行提示"曾被打回（原因）" → 成功后清除
- 全量 25 脚本 py_compile PASS

### 遗留（P2+）

- 质量自评分重写闭环（quality<6 自动轻量重写）
- 30 万字分卷输出、设定集跨书复用、多平台导出
- 出场记录 stage4 实时增量（P1.6 方案 2.6 节）

---

## 2026-09-13 开发记录（方向1：素材管线强化 — 原始碎片 → 结构化卡片）

### 目标

用户有一批**自由命名、无统一格式**的原始创作碎片（只为他自己记忆服务）。要在不污染
`materials/raw/`（结构化卡片）的前提下，把碎片接进 stage1 归并链路，并提供 GUI 勾选→一键成卡。
三条用户决定：① 碎片自由命名（聚类不能靠文件名）；② `materials/original_scraps/` 不进 Git；
③ `gates.auto_rewrite` 默认关闭（方向3 暂缓）。

### 1. 碎片聚类（确定性，零 LLM）✅

**新增** `scripts/utils/scrap_cluster.py`：
- 时间戳三级回退：文件名任意位置 → 正文前 5 行 → 文件 mtime（标 `mtime` 表示不可信）；
  额外支持整名纯数字 `0312`（月日）/ `20250406`（年月日），仅当文件名**整体**是数字时生效
- 聚类**内容驱动**：中文 2-4 gram 共现词 + difflib 相似度 + 并查集；成簇判据=共享特征词 ≥2 个，
  或共享 1 个长词（≥3 字），或共享 1 个仅这两条碎片出现的专属词；文件名语义片段仅作弱提示
- 前瞻备忘识别（待定/以后再/TODO…），归并时不得当既成事实
- 输出 `data/setting/scraps_index.json`：簇 + 合并证据（可审计）+ 时间轴 + 前瞻 + 内联分批建议
- `content_fingerprint`：只对碎片**内容**取 sha256（不含 generated_at），供 stage1 判变化

### 2. stage1 接入 ✅

**修改** `scripts/stage1_consolidate.py`：
- 新增 `--scraps/--with-scraps/--no-scraps`；`prepare_scraps` / `build_scraps_task` /
  `validate_scraps_merge` / `merge_scraps_batches` / `run_scraps_merge`
- **指纹修正（最关键）**：`manifest_fingerprint(manifest, extra_fingerprints=())` 把碎片内容指纹
  一并折进素材指纹 —— 否则改碎片永远命中"素材无变化，复用设定集"，功能等于没接线
- 碎片提炼产物 `data/setting/scraps_merge.json` 作为**额外输入**喂给既有归并任务
- 超 180000 字符按碎片分批（llm_client 上限 220000，且超限只打 WARN 静默丢文件）
- 碎片为空 → 完全退化旧流程；`materials/raw` 为空但有碎片 → 仅用碎片归并（不再直接判失败）
- **新增** `prompts/stage1_scraps.md`；`prompts/stage1_materials.md` 加 `{{extra_inputs}}`（空串安全）
- `utils/fake_client.py` 加 `stage1_scraps` 分支（fake 全链不卡）
- `config/project.yaml` 新增 `materials:` 段（dir / scraps_dir / use_scraps）

### 3. GUI + 端点 ✅

- `scripts/nf_api.py` 新增 `/scraps/list`（按簇分组+统计+索引陈旧提示）、`/scraps/read`、
  `/scraps/save`（写前备份）、`/scraps/delete`（需 confirm + 先快照 + 备份）、
  `/scraps/promote`（**由 Python 落盘** materials/raw，模型写白名单只有 data/**）
- **新增** `console/src/ScrapsPanel.vue`：簇分组列表（含合并证据、前瞻徽章、排除词计数）、
  原文逐行勾选、手工追加信息点、卡片名/类型、"生成本地卡片"（零 LLM）、
  勾选态存 localStorage；`App.vue` 素材页签加子页签「结构化卡片 / 原始碎片」

### 4. 顺手修掉的既有 bug

- `nf_api` 的 `/materials/remerge` 注释写着"清 fingerprint"但代码没清 → 素材无变化时按钮静默
  "复用设定集"，名不副实 → 已补上真实清指纹

### 验证汇总

- `Temp/test_scrap_cluster.py`：**44 断言全过**（自由命名 6 种风格 / 时间戳三级+数字名 /
  内容聚类 / 反向用例不误并 / 否定语境 / 前瞻 / 指纹稳定性 / 编码回退 / 分批 / 空目录）
- `Temp/test_stage1_scraps.py`：**30 断言全过**（临时工作目录 fake 全链，真实 data/ 零污染；
  含"改碎片→重归并""仅改 mtime→复用""超限分批+id 重编号"）
- `Temp/test_scraps_api_http.py`：**29 断言全过**（临时项目根起 nf_api，覆盖 save/delete/promote
  与确认门、路径穿越、非法类型；真实仓库零触碰）
- `console` `npm run build` 通过；6 个改动脚本 py_compile 通过
- 真实数据未被触碰（当前书档《留下你的歌》产物完好）

### 遗留

- `nf_api_selftest.py` 已补碎片只读端点用例，但**未执行**：它会清空 `data/` 与 `logs/`，
  当前仓库有真实书档，需 `--force`（=销毁当前书档，快照可回退），待用户确认
- 碎片→卡片的「AI 提炼」通道有意推迟（stage1 归并已用 LLM 消化碎片，GUI 再调一次属重复付费；
  本地拼卡零成本）。如需：加 `prompts/scrap_promote.md` + fake 分支 + `mode:"ai"` 参数
- 方向2（角色关系可视化）、方向3（质量自评闭环）未开工；`gates.auto_rewrite` 默认关闭

---

## 2026-09-13 开发记录（GUI 可用性修复：竖排文本 / 拥挤感 / 返回收件箱）

### 症状与根因

用户反馈 4 个问题，其中"大纲树文本竖排"是**真 bug 不是审美问题**：

`console/src/style.css` 里有两处 `.tree-item` 定义。后一处（第 505-558 行）是已移除的
「项目结构树」功能留下的**死代码**（`.project-tree` / `.tree-group*` / `.tree-item-*` /
`.tree-status-*`，全项目含动态拼接均无引用），它把 `.tree-item` 覆盖成 `display:flex`。
于是 `OutlineTree.vue` 的节点容器 `<li class="tree-item">` 变成 flex 行，嵌套的 `<ul>`
与 `.tree-row` 并排抢宽度 → `.tree-label`（flex:1 + min-width:0）被压到只剩一个字宽
→ 逐字换行，看起来就是竖排文本。

### 修复清单

1. **删除死 CSS 块**，并就地留注释说明"勿再覆盖 `.tree-item`"（防复发）；树样式统一到一处
2. **树可读性**：`padding 3px→6px 8px`、`line-height 1.55→1.65`、折叠箭头 13→17px 且有 hover
   反馈、深层缩进加虚线引导、`.tree-edit` 加高到 60px
3. **大纲树第二个真问题**：act 节点把**整段正文**当标题显示（实测最长一条 31 行）。
   显示用 `tail` 截断到 44 字，**完整原文另存 `fullTail`** 用于悬浮提示与内联编辑预填
   —— 否则"双击编辑再直接保存"会把用户原文截掉（数据安全陷阱）。完整文本仍可在右侧
   属性面板编辑
4. **大纲页布局**：左树右详情由 grid 改为 flex，新增**可拖拽分隔条**（默认 42%、下限 260px、
   上限 78%，宽度存 localStorage，可双击/按钮复位），树滚动区 62vh→72vh
5. **流水线页紧凑度**：阶段行 padding 9→13px、按钮 padding 4px11px→6px13px（点击高度 33px）、
   pill 加高、`.run-all-bar` 补上缺失的横向布局样式（此前无任何样式，纯堆叠）、
   内容区宽度 1180→1320px、按钮加 `:disabled` 视觉
6. **返回收件箱**：新增「来路」标记，从收件箱跳到大纲/分章后内容区顶部出现
   `← 返回收件箱`（带待审数量提示），回到收件箱自动消失；正常浏览其他页签不受打扰
7. **启动器乱码**：乱码来自 `console/main/index.js` 主进程的**中文 console 输出**
   （GBK 控制台），与 bat 无关。用户在 89827ca 已定调"纯 ASCII bat"（chcp 65001 会让
   批处理中文 echo 出错），故按同一思路把主进程 console.* 全部改 ASCII（14+4 处）；
   `nf_api_child.log` 与浏览器 `alert()` 的中文照旧（前者是 UTF-8 文件、后者由浏览器渲染）
8. **顺手修**：`setupAutoUpdater()` 增加 `!app.isPackaged` 提前返回，消除源码直跑时
   electron-updater 打的 "application is not packed" 干扰日志

### 验证方式（新增可复用工具）

`Temp/verify_ui_fix.js`：headless Chrome + CDP（用系统 Chrome，不下载 Chromium）
驱动构建产物，把"看起来对不对"变成**可量化断言**：

- 竖向故障量化：`.tree-label` 最窄宽度 **1 字宽 → 519px**；最长节点 **31 行 → 2 行**
- 数据未丢：最长悬浮提示 1067 字（截断只影响显示）
- 拖拽调宽：646 → 758px 且写入 localStorage
- 收件箱→大纲→返回：返回按钮出现/消失均正确
- 无未捕获 JS 错误
- 结果：**15 项断言全过**，截图 `Temp/fix_1..4_*.png`

踩坑记录：环境里设了 `HTTP_PROXY/HTTPS_PROXY`，localhost 请求会被代理吞掉返回 502，
headless Chrome 必须加 `--no-proxy-server`；`require('ws')` 按脚本自身目录解析，
需 `NODE_PATH=console/node_modules`；后台服务要用工具的后台模式起，`&` 在调用间不保活。

### 并发改动说明

本次发现仓库中有用户自己提交的 `39b785f feat: 一键启动绒花墨坊控制台（.bat）` 与
`89827ca fix: pure-ASCII bat`。已**还原**我对 bat 的改动以示尊重（不覆盖已验证的决定），
启动器乱码改从主进程日志侧解决。

### 遗留

- 用户提出的「项目」「设置」页外观配置（字体自定义、字号自定义、主题配置）——用户明确
  说不着急，未开工
- 分章页"能看但没有编辑性与互动性"——用户表示可接受，未动
- `开发计划_三方向.md` 在工作区被删除（未提交），原因未知，待用户确认是否保留

---

## P3 多平台发布：Markdown 分卷导出（2026-09-13）

**交付物**：
- `scripts/stage8_markdown_export.py`：将 refined/checked/raw 章节按卷拆分（默认 5 章/卷），每卷独立目录含卷首目录 + 章节正文 + README 元数据
- API：`POST /export/markdown {per_vol, book_name}`
- GUI 导出面板（顶部"导出"标签）：格式选择（MD+Word / 仅 MD / 仅 Word）、每卷章节数配置、最近导出记录
- 全书合并 Markdown 一并生成（`全书.md`）

**输出结构示例**：
```
output/留下你的歌_markdown/
├── 全书.md
├── 卷01_章节01-05/
│   ├── 卷01.md
│   └── README.md
└── 卷02_章节06-10/
    ├── 卷02.md
    └── README.md
```

## P2.4 知识库 embedding 接入（2026-09-13，倒排索引）

**设计取舍**：ROSA 是结构化词条库（每篇独立 markdown + frontmatter），精确关键词匹配 >> 语义向量。倒排索引零依赖、毫秒级响应、"露汐"不会匹配到"露西亚"。

**交付物**：
- `scripts/utils/kb_index.py`：
  - `build_index(vault_path, out_path)` → 倒排索引构建（中文 2-4 gram + 英文词，过滤停用词）
  - `load_index() / search(query, top_k, vault_path)` → 检索
  - `get_snippet(entry_path, max_chars)` → 读取词条摘要
  - CLI：`python scripts/utils/kb_index.py build/search`
- `data/state/kb_index.pkl`：1318 词条，3523131 词项，88MB
- API：
  - `GET /kb/search?q=露汐&top=5` → 检索知识库词条（含 snippet + 相关度）
  - `POST /kb/build` → 后台重建索引
- 写作链路注入：`stage4_writing.py` 中用本章大纲关键词检索知识库，相关词条摘要注入任务文件

**检索结果示例**：
- "露汐" → 露汐（03 设定/01 人物/02 新作人物/露汐.md）score 3
- "月神" → 月神（03 设定/01 人物/02 新作人物/月神.md）score 3

## 方向3：质量自评闭环（2026-09-13）

**背景**：三方向开发计划的第 3 项（原估 4h）。stage4 早已写入
`progress.stages["4"].needs_rewrite`（`quality < chapter.quality_threshold`，默认 6），
但注释明确写着「P0 只标记不重写」——本次把**已有标记变成动作**，不重复实现检测逻辑。

### 交付物

1. `scripts/auto_rewrite.py`（新）：薄封装 `batch_refine.run_single_refine`，
   白拿「备份 `data/chapters/history/chNN_vM.md` → 建 `stage4_refine` 任务 →
   跑模型 → ±20% 铁律校验」，不重写任何一条既有逻辑
   - `collect_targets(progress, threshold, only)`：**优先**读 `needs_rewrite`，
     **兜底**重扫章节文件的 `<!-- quality: X/10 -->`（该列表只在 stage4 当次写入，
     历史 run 可能没留）；最后按**当前文件实际 quality** 过滤，已被修好的自动剔除
   - `build_feedback(chapter, quality, threshold)`：默认要求只在文风/细节层面收敛补强，
     并**叠加 `data/outline/review_report.json` 的确定性发现**（让重写吃上审查结论）
   - `run_auto_rewrite(...) -> (ok, msg, stats)`：逐章调度；单章失败不阻断；
     预算熔断即中止；成本经 `_RecordingClient` 代理回传，复用当前 `run_id` 记账，
     使 `cost_report.py --by-chapter` 能看到重写开销
   - 报告 `data/outline/auto_rewrite_report.md`（人可读，每次运行追加）+
     `auto_rewrite_report.json`（`latest` + `runs` 保留最近 50 次）
2. `config/system.yaml`：新增 `gates.auto_rewrite: false`（**默认关**，与既有
   `auto_refine: false` 保持同一克制姿态）、`gates.auto_rewrite_max_rounds: 1`
3. `scripts/orchestrator.py`：n==4 钩子**排在审稿分支之前**（否则审查看到的还是旧稿）；
   新增 `_warn_rewrite_conflict()`：与 `gates.auto_refine` 同时开启时启动即告警
   （两套会改同一批章节，以 auto_rewrite 为先）
4. `scripts/utils/fake_client.py`：新增 `batch_refine` 分支——保留字数、把 quality
   置 8/10，使「重写 → 复评 → 幂等」链路可端到端验证（此前会落到 `_write_generic`
   写出「（fake 产物）」，字数崩塌到 ±20% 之外必失败）
5. `scripts/nf_api.py`：`stage_status()` 暴露 `auto_rewritten`；
   新增 `POST /auto_rewrite/run {threshold?, max_rounds?, chapters?, dry_run?}`
   （**dry_run 默认 true**，须显式 `dry_run:false` 才真正改稿）
6. `AGENTS.md`：命令速查 + 关键路径 + 运维场景补条目

### 幂等设计

`progress.stages["4"].auto_rewritten = {章号: 已用轮次}`。轮次用尽后不再自动重试，
保留 `needs_rewrite` 标记转人工（走 `batch_refine.py` 通道）。dry-run **只读**已有轮次，
不创建键、不消耗轮次、不改章节。

### 验证

| 自检 | 结果 | 覆盖 |
|------|------|------|
| `Temp/test_auto_rewrite.py` | **43 PASS / 0 FAIL** | 目标收集（优先/兜底/达标剔除/限章）、dry-run 无副作用、完整重写（提质+清标记+双报告+±20%）、幂等、轮次上限、预算熔断、反馈叠加审稿发现、orchestrator 开关与冲突告警、**n==4 钩子真接线（monkeypatch 计数）** |
| `Temp/test_auto_rewrite_api_http.py` | **17 PASS / 0 FAIL** | `/state` 字段位、`POST /auto_rewrite/run` 默认 dry-run 不改稿、显式执行提质、达上限幂等 |
| 回归 | 全绿 | `test_scrap_cluster` 44、`test_stage1_scraps` 30、`test_scraps_api_http` 29、`test_style_v2`/`test_style_drift` OK |

两个新自检都在**临时目录**跑（复制 scripts/config/prompts 当 ROOT），真实 `data/` 零污染。

### 踩坑

- `setdefault("auto_rewritten", {})` 会在 dry-run 时也创建键 → 改为 dry-run 只 `get`
  （首版自检第一条失败就是它）
- `batch_refine` 的 fake 分支缺失：任务名 `batch_refine_chNN.md` 不匹配 `stage4_ch*`，
  会落到 `_write_generic` 写「（fake 产物）」→ 字数崩塌、±20% 必失败

### 未跑项（需用户确认）

- `scripts/nf_api_selftest.py` 会**清空 `data/` 与 `logs/`**，而仓库里有《留下你的歌》
  的真实产物（setting.json、chapters/raw/*.md）→ 未经确认不执行；
  新增端点的等价覆盖已由 `Temp/test_auto_rewrite_api_http.py` 在临时项目根内完成
- `gates.auto_rewrite` 保持 **false**，未获用户确认不开自动改稿

