# 2026-09-11 工作日志

## 绒花墨坊开发派单（20260911-2055）四项任务全部完成

派单优先级：任务1 → 任务2 → 任务4 → 任务3。

### 新增文件
- `scripts/utils/outline_struct.py` — 大纲结构化底座（解析/渲染/局部替换），纯结构不做评分
  - `parse_global` / `render_global` / `replace_entry` / `build_char_index` / `match_chars`
  - 关键正则：`NODE_LINE_RE`（节点N：）/ `PLAN_LINE_RE`（第N章：）/ `_SECTION_RE`
- `scripts/utils/outline_panel.py` — GUI 面板服务层（可被 CLI 复用）
  - `build_structure`（解析+贴常驻评分）、`_assign_plan_acts`（章节规划按节点区间归 act：前25%起/50%承/75%转/其余合）
  - `apply_node_refine` / `clean_entry_output` / `undo_last_refine`（任务2）
  - `list_versions` / `resolve_version` / `diff_outlines` / `char_diff`（任务4）
  - `draft_path` / `list_drafts` / `cleanup_drafts` / `compose_from_drafts`（任务3）
- `prompts/stage2_refine_node.md` — 单节点精修模板（frontmatter max_tokens 2000 / temperature 0.4）
- `console/src/OutlineTree.vue` — 手写递归树（折叠/展开/选中/拖拽/双击内联编辑）
- `console/src/OutlineView.vue` — 「大纲」页签宿主（树+属性面板+工具栏+版本对比+多方案）

### 修改文件
- `scripts/nf_api.py`
  - **CORS 修复（重要，属预先存在的阻塞性 bug）**：新增 `ALLOW_ORIGIN_RE` + `_cors()` + `do_OPTIONS()`（204）；
    修复前 Electron `file://` 场景所有 API 调用失败（GUI 显示"离线"），本次一并解决
  - 新增 endpoints：GET `/outline/structure|history|diff|drafts`；POST `/outline/save|restore|compose|drafts/cleanup`、
    `/refine/outline/node|undo`、`/stage/2/run-multi`
  - 补 `GLOBAL`/`HISTORY_DIR` 常量（修过 NameError）
- `scripts/utils/fake_client.py`
  - `_write_global` 改为优先遵循任务文本里的「写入文件: xxx.md」（与 `_write_chapter` 一致），
    使多方案 draft 能落到各自文件
- `console/src/diff.js` — 追加 `parseOutline`/`charDiff`/`diffOutlines`/`diffSeq`/`lcsOps`；
  **`diffParagraphs()` 原封不动（红线）**
- `console/src/App.vue` — 页签栏「章节」与「审稿」之间插入「大纲」页签
- `console/src/style.css` — 追加 `.ov-*` / `.tree*` / `.drawer-wide` / `.drawer-xwide` 等样式

### 本轮修复的两个真实 bug
1. `render_global` 的 title 归一化分支混乱 → 拼合后一级标题丢失 `#` 前缀。
   改为：去 `#` → 补「整体大纲」→ 加 `#`，保留用户既有标题风格（如 `# 测试书 整体大纲`）。
2. `act_stage2_run_multi` 原实现会真实覆盖 `global.md` 再从 draft_1 恢复（中途失败即丢原稿）。
   改为把任务模板的 `{{path_global_outline}}` 逐份指向 draft 文件，**全程不触碰 global.md**（已验证 md5 前后一致）。

### 验证
- 27 项综合验收全部通过（任务1 结构/评分、任务2 清洗/undo、任务4 diff/版本、任务3 draft/拼合、6 项红线）
- 10 项回归测试通过（parse→render→parse 往返、replace_entry、check_global_outline、review 字段、diff 自比对）
- HTTP 冒烟：OPTIONS 204 + CORS 头正确；缺参 400；不存在版本 404
- 前端 `vite build` 通过（16 modules，CSS 19.51KB，JS 128.43KB）
- CDP headless Chrome 截图确认四个视图渲染正常

### 数据状态
- `data/outline/global.md` 已确认与原始备份 md5 一致（未被测试污染）
- `data/outline/history/` 已清空测试版本；无 draft 残留

### 未做（如需可续）
- 真实 LLM（非 fake）跑通 `/stage/2/run-multi` 三方案生成
