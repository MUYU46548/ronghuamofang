# =========================================================
# NovelForge 项目级环境变量模板
# 复制为 .env 后按需填写；.env 已被 .gitignore 排除，绝不提交
# =========================================================

# 模型 API 凭据由 Hermes 统一管理（~/.hermes/.env），本项目无需重复配置。

# 预算上限（元），超过后 orchestrator 自动熔断暂停
# BUDGET_LIMIT_YUAN=300

# 可选：第三方服务密钥（如阶段 7 需要外部工具、翻译 API 等）再启用
# EXTRA_API_KEY=
