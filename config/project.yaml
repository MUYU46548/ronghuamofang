# =========================================================
# NovelForge 项目信息（每次运行前可修改）
# 参考：架构文档 v2 4.2 / 十一
# =========================================================

book:
  name: "测试用例"        # 书名
  genre: "奇幻、科幻"                    # 类型
  target_words: 20000            # 全书目标字数
  style: "奇幻日常、白描勾画"           # 风格（如：偏治愈日常、史诗群像）
  chapters: 3                    # 本次运行章节数
  language: "zh-CN"
  # 用户提供的大纲/梗概/核心构思（可选）。留空则系统基于素材自拟。
  # 多行写法：user_outline: |
  #   第一句点明核心设定……
  #   第二句点明主角弧光……
  user_outline: ""

vault:
  readonly: true                  # ROSA 设定库只读（系统永不写入 E:/图书馆/ROSA）
  vault_links: "materials/vault_links.md"  # 设定库引用索引（阶段1 生成/维护）

run:
  output_word: true               # 阶段7 是否转 Word（false 则仅输出 merged Markdown）
