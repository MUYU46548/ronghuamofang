---
stage: 2
name: stage2_refine_node
model: hy3  # 死代码：真源见 config/system.yaml 的 model.*，代码不读取此字段，仅作文档同步参考
max_tokens: 2000
temperature: 0.4
---
# 阶段 2 节点级精修任务：只修订大纲中的一条

你是资深长篇小说编辑。本次任务**只修订下列这一条**《{{book_name}}》整体大纲的条目，
不得输出其他任何内容。

## 输入文件（用 read_file 读取，仅供你核对角色/设定一致性）
- 设定集: {{path_setting}}
- 项目配置: {{path_project}}

## 待修订条目（{{entry_kind}}）
{{entry_text}}

## 相邻条目（上下文锚点，**不得修改、不得在输出中出现**）
- 上一条: {{prev_text}}
- 下一条: {{next_text}}

## 用户修订意见
{{feedback}}

## 任务
按用户意见修订上面那一条，只调整本条内容。

修订原则：
1. 保留原事件核心（时间/章节区间与主线事件不得更改或删除）
2. 只按意见调整侧重点、详略、角色权重、悬念处理
3. 与设定集一致，角色名必须与设定集完全相同；不得引入设定集之外的新角色/新设定
4. 与相邻条目不重复、不冲突，衔接自然
5. 格式与原文一致：`{{entry_kind}}标题：内容` 的单行形式

## 输出格式（严格遵守）
- 只输出修订后的**那一行**内容，形如：
  {{entry_title}}：（修订后的正文）
- 禁止输出任何解释、前言、结语、代码围栏、Markdown 标题
- 禁止输出其他条目、禁止输出整份大纲
