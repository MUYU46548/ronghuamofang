# NovelForge 三方向开发计划（v1，已对代码校准）

> 目标：把「素材管线强化」「角色关系可视化」「质量自评闭环」三个方向拆到可执行粒度。
> 本文所有接口名、函数名、路径均已与仓库当前代码核对（主分支 `main`，HEAD `5ae6e74`）。
> 总工时估算：**约 30h ≈ 3.75 个工作日**（含 1h 回归验证）。

## 状态（2026-09-13）

- **方向1 已落地**（1.1–1.4 + 回归），方向2/3 未开工。
- 用户决定：碎片**自由命名**（无固定格式）→ 聚类主信号改为**内容共现**，文件名前缀降级为弱提示；
  `materials/original_scraps/` **不进 Git**（已记入 `.gitignore`，后续做私人数据导入导出）；
  `gates.auto_rewrite` **默认关闭**。
- 方向1 的实现与原计划的偏差见文末「附录：实现记录」。

---

## 0. 关键前置发现（先读，直接影响各方向设计）

| # | 发现 | 影响 |
|---|------|------|
| F1 | `stage1_consolidate.manifest_fingerprint()` 只对 `materials/raw` 归一化内容做 sha256；判据是 `if setting_valid and sig == prev_sig → 复用设定集` | 碎片若**不进指纹**，改碎片永远不触发重归并。方向1 必须把 scraps 折进指纹，否则功能等于没接线 |
| F2 | `llm_client.allowed_paths()` 只放行 `data/**` 与 `logs/runs.db`，越界直接拒绝落盘 | 「碎片→结构化卡片」**不能由模型直接写 `materials/raw/*.md`**。必须模型产出到 `data/`，再由 Python 落盘到 `materials/raw/` |
| F3 | 直连引擎的目录内联有硬格式：`inline_inputs()` 靠 `DIR_SPEC_RE = "下的\s*[*][.]md"` 识别目录，且只 `glob("*.md")`，总上限 220000 字符，超限仅 WARN 后静默跳过 | 模板里碎片目录必须写成 `- 原始碎片目录: {{path_scraps}}/ 下的 *.md`；碎片总量超限要提前分批，否则"悄悄少读一半素材" |
| F4 | `stage4_writing.py` 已经写入 `progress.stages["4"]["needs_rewrite"]`（`quality < cfg.chapter.quality_threshold`，默认 6），注释明确写着「P0 只标记不重写」 | 方向3 不是新造检测，而是**把已有标记变成动作**。不要重复实现检测逻辑 |
| F5 | `orchestrator.run()` 的 `n == 4` 钩子已被占用：`review_after_stage4: true` → 跑 `chapter_review` 并 `return 3`（等待审阅） | auto_rewrite 挂 n==4 必须与它排队，否则两套修稿路径打架 |
| F6 | `batch_refine.run_single_refine(cfg, proj, chapter, feedback, client, task_dir, dry_run) → (ok, msg, backup)` 已实现「备份 history/chNN_vM.md → 建 `stage4_refine` 任务 → run_task → ±20% 铁律校验」 | 方向3 直接复用它，工时从 3h 降到 1.5h，且天然满足铁律 |
| F7 | `config/project.yaml` **没有** `materials:` 段，`stage1` 用 `proj.get("materials", {}).get("dir", "materials/raw")` 取默认值 | 需新增配置段；按行改写保留注释的范式可抄 `utils/project_config.set_style_notes()`（含备份 + 回读校验） |
| F8 | `.gitignore` 只忽略 `data/ history/ output/ Temp/ console/node_modules|dist`，**`materials/` 受版本管理** | 新建 `materials/original_scraps/` 会随仓库走。若放的是私人原始碎片，需显式决定是否加忽略 |
| F9 | `data/state/appearances.json` 已存在且有数据，但 `nf_api.py` **没有任何端点暴露它**（只有 `/setting/current`） | 方向2「数据接入」漏了后端一个只读端点，原表只算了前端 2h |
| F10 | `nf_api_selftest.py` 会**清空 `data/` 与 `logs/`** 后以 fake 模式跑用例（AGENTS.md 已标 ⚠️） | 跑回归前必须先 `snapshot.py`；新增端点要补进该自测 |

---

## 1. 方向1：素材管线强化（约 19h）

### 1.0 数据流设计（先定骨架，避免边写边改）

```
materials/original_scraps/*.md            ← 人类乱写（含时间戳、前瞻备忘）
        │
        │ ① scrap_cluster.py（纯确定性，零 LLM，可单测）
        ▼
data/setting/scraps_index.json            ← 聚类 + 时间序列 + 前瞻标记 + 每簇摘要
        │
        │ ② stage1 --scraps（LLM 归并，输入内联）
        ▼
data/setting/scraps_merge.json            ← 模型产出的「信息点 / 冲突 / 待确认项」
        │
        │ ③ 作为额外输入喂给既有 stage1_materials.md 归并任务
        ▼
data/setting/setting.json（既有链路不变）

并行支线（GUI）：
ScrapsPanel.vue 勾选信息点 ──POST /scraps/promote──▶ Python 落盘 materials/raw/{名}_{类型}卡.md
                                                        └→ 下次跑 stage1 自动进入归并
```

设计要点：**碎片不进 `materials/raw/`**（守你的纪律），只在归并阶段作为额外输入；碎片→卡片的落盘一律走 Python，因为 F2。

---

### 1.1 新建 `materials/original_scraps/`（0.5h）

- 新建目录 + `.gitkeep`（保证空目录能进版本）。
- 在 `AGENTS.md`「关键路径」补 1 行说明：`materials/original_scraps/` = 原始创作碎片（人写，不直接被管线消费）；`materials/raw/` = 结构化素材卡（管线消费）。
- 把 OB34 原始碎片放入。若这些文件此前已误放在 `materials/raw/`：**先 `python scripts/snapshot.py "before_scraps_move"` + 列清单 + 用户确认**，再移动（项目硬性约束：破坏性操作前 snapshot + confirm）。
- 验收：`ls materials/original_scraps/` 有碎片；`python scripts/stage1_consolidate.py` 行为与改动前一致（碎片目录不参与）。

---

### 1.2 `scripts/utils/scrap_cluster.py`（新建，5h）

纯确定性模块，不引 LLM、不依赖网络，便于单测。

**接口设计**

```python
TIMESTAMP_RE  = re.compile(r"^\s*\[?\(?\s*(\d{4})\s*[-/.年]\s*(\d{1,2})\s*[-/.月]\s*(\d{1,2})")
PREFIX_SEP_RE = re.compile(r"[_\-—·．.\s【\[(（]")
LOOKAHEAD_RE  = re.compile(r"(待定|待确认|存疑|待补|TODO|后续|暂时|先这样|回头|未定|\?\?+)")

@dataclass
class Scrap:
    path: str; stem: str; prefix: str; cluster: str
    ts: str | None            # 归一化 "YYYY-MM-DD"
    ts_source: str            # "filename" | "content" | "mtime"
    order_key: tuple          # 排序键
    chars: int; lines: int
    lookahead: list[dict]     # [{line_no, sentence}] ≤3 条
    excerpt: str              # 前 200 字（GUI 列表用）

def normalize_timestamp(text: str, filename: str, mtime: float) -> tuple[str | None, str]
def cluster_key(stem: str) -> str                    # 剥时间戳后取首个分隔符前的 token
def scan_scraps(dir_: str = "materials/original_scraps") -> list[Scrap]
def build_index(scraps: list[Scrap]) -> dict          # 簇分组 + 全局时间线 + 前瞻汇总
def write_index(index: dict, out: str = "data/setting/scraps_index.json") -> str
def main()  # argparse: --dir --out --json
```

**实现细节**

1. **时间戳归一化**（原表 2h）：正则 `^\s*\[?\(?(\d{4})[-/.年](\d{1,2})[-/.月](\d{1,2})`。
   - 文件首行命中 → `ts_source="filename"`；否则扫文件正文前 3 行 → `"content"`；都无 → 回退 `os.path.getmtime()` 并标 `"mtime"`（GUI 上淡显，提醒不可信）。
   - 无年份的 `3-5`/`3月5日` 形式：用**同簇最早年份**补全（簇内先解出有年份的样本）。
   - 基准 = 全碎片的**最小 ts**，索引里另存 `base_ts` 供"相对第 N 天"展示。
2. **跨碎片聚类**（原表 3h）：**内容驱动**——中文 2-4 gram 共现词（经"极大 n-gram 归并"+虚词过滤）
   + difflib 文本相似度 + 并查集合并；成簇判据：共享特征词 ≥2 个，或共享 1 个长词（≥3 字），
   或共享 1 个只在这两条碎片里出现的专属词。文件名语义片段只作**弱提示**（`HINT_ONLY_SCORE=False`，
   单独命中不足以成簇），因为自由命名不可靠。
   - 否定语境处理：碎片里常写"这和月神**无关**"做区分，这类词会被识别并剔除（`negated_tokens`），
     否则会把不相干的碎片并进来；同时 df 过滤**不做**"出现率 >60% 就丢弃"的激进过滤
     （小语料里主角名本就高频，激进过滤会让该合的簇合不上）。
   - 兜底开关 `--cluster-by content`：已内建为默认路径，无需另开。
3. **前瞻备忘识别**（原表已含）：行级命中 `LOOKAHEAD_RE`，截取 ≤120 字，跳过代码围栏/引用行；汇总到 `index["lookaheads"]`。
4. **单测**：新建 `Temp/test_scrap_cluster.py`（与仓库既有 `Temp/test_style_v2.py` 同风格），覆盖：中式/ISO/斜杠时间戳、无年补全、无日期回退 mtime、前缀聚簇、空目录、超长单行、`--cluster-by content`。
5. **验收**：对真实 OB34 碎片跑出 `scraps_index.json`，聚类结果与人工判断一致；**这一步要先给用户看结果再往下写**（碎片命名规律目前是未知数）。

---

### 1.3 `stage1_consolidate.py` 加 `--scraps`（5.5h，原估 4h）

**改动点（精确到函数）**

| 位置 | 改动 |
|------|------|
| `main()` | 加 `--scraps`（默认 `materials/original_scraps`）、`--with-scraps`（开关）、`--no-scraps`；`--scraps` 时先跑 `scrap_cluster` 生成索引 |
| 新增 `load_scraps(scraps_dir, index_path)` | 调 `scrap_cluster.scan_scraps/build_index/write_index`，返回 index；**先统计总字符数**，超 180000 就按簇切批（见风险） |
| 新增 `build_scraps_task(proj, index_path, scraps_dir)` | `load_template("stage1_scraps.md", {...})` |
| 新增 `validate_scraps_merge(path)` | JSON 可解析 + 顶层含 `points`/`conflicts`/`open_questions` |
| `build_setting_task()` | 加可选参 `extra_inputs=()`，模板新增 `{{extra_inputs}}` 占位（空串安全，不破坏旧行为） |
| `run_stage()` | 若 scraps 非空 → ① 先跑 scraps 归并（模型写 `data/setting/scraps_merge.json`）；② 把该文件作为 `extra_inputs` 喂给既有归并任务 |
| `manifest_fingerprint()` | **加 `extra_paths=()` 参数**，把 `scraps_index.json` 内容一并进 sha256（F1 的关键修正） |

**新增提示词 `prompts/stage1_scraps.md`**（守「提示词与代码分离」铁律），`输入文件` 段必须写成 F3 要求的格式：

```
## 输入文件（用 read_file 读取）
- 原始碎片目录: {{path_scraps}}/ 下的 *.md
- 碎片索引（聚类与时间序）: {{path_index}}
- 结构化素材卡目录: {{path_normalized}}/ 下的 *.md
```

输出用 `===FILE: data/setting/scraps_merge.json===` + `===END===` 协议（白名单内）。内容形如：

```json
{"points":[{"id":"p001","cluster":"月神","ts":"2024-03-05","content":"...","suggest_card":"角色卡","confidence":"high"}],
 "conflicts":[{"a":"...","b":"...","note":"取更详细的一条"}],
 "open_questions":["..."]}
```

**fake 模式适配**：`utils/fake_client.py` 需加 `stage1_scraps` 分支（返回一个最小 `scraps_merge.json`），否则 `NF_API_ALLOW_FAKE=1` 全链测试会卡在 stage1。

**验收**

1. `python scripts/stage1_consolidate.py --with-scraps` 生成索引 + `scraps_merge.json`，设定集仍通过 `validate_setting()`；
2. 改一个碎片文件重跑 → 日志出现「素材有变化（指纹不一致），重新归并设定集」；
3. 不改任何文件重跑 → 出现「素材无变化，复用设定集」；
4. `--no-scraps` 行为与改动前**逐字节一致**（回归红线）。

**风险**：碎片总量超 220000 字符时 `inline_inputs` 静默截断 → 在 `load_scraps` 里预检总量，超限则按簇拆成多个任务（每任务只内联本簇），并把 WARN 升级为明确日志「本批跳过 N 个碎片」。

---

### 1.4 碎片→卡片 GUI 面板（7h，原估 6h）

**挂载点**：`console/src/App.vue` 素材页签（`v-if="tab === 'materials'"`）内新增 sub-tab，抄现有 `settingTab` 的 `tabs-sub` 写法：`结构化卡片 | 原始碎片`。

**后端：`scripts/nf_api.py` 新增 4 个端点**（仿 `/materials/*` 的 `elif p == "..."` 写法，复用 `MaterialsManager`，它已支持任意 raw_dir）

| 方法 | 路径 | 入参 | 返回 |
|------|------|------|------|
| GET | `/scraps/list` | – | `{dir, count, clusters:[{key, base_ts, items:[Scrap]}]}` |
| GET | `/scraps/read` | `?name=` | `{ok, name, content}` |
| POST | `/scraps/save` | `{name, content}` | `{ok, size, backup}` |
| POST | `/scraps/promote` | `{name, points, card_type, card_name}` | `{ok, path, backup}` |

`/scraps/save` 与 `/scraps/promote` 都要**写前自动备份**（碎片被改写前存 `materials/original_scraps/_backup/`）。

**`/scraps/promote` 的落盘链路（F2 的落地）**

1. 拼任务 `data/state/tasks/scrap_promote_<name>.md`，要求模型输出
   `===FILE: data/setting/scrap_promote.json===`（`{card_name, card_type, body_md, source_points}`）；
2. Python 读该 JSON → **先备份**同名 `materials/raw/<card_name>.md` 到 `materials/raw/_backup/<名>_<时间戳>.md` → 再写卡片；
3. 返回卡片路径 + 备份路径，GUI 提示「已生成 materials/raw/xxx，下次跑 stage1 生效」。

**降级路径（建议同做，成本很低）**：提供「本地拼卡」按钮，直接把勾选的信息点按卡片模板拼成本地 Markdown，**0 LLM、0 成本**。AI 提炼失败/额度紧张时可用。

**交互**

- 左栏：簇分组折叠树，每条显示 `ts（时间轴相对天数）+ 前瞻徽章 + 字数`；右栏：原文预览（复用 `window.mofangAPI.readPreview` 的模式）。
- 勾选粒度：按行 + 按整段；勾选态存 `localStorage`（防误刷新丢失）。
- 删除碎片：**不直接 unlink** → 先 `snapshot.py` + GUI `confirm`（项目硬性约束）。

**验收**：`npm run dev` 下 素材 → 原始碎片 可列表/预览/勾选/保存；「本地拼卡」产出可读的 `materials/raw/xxx_角色卡.md`；「AI 提炼」在 fake 模式下跑通；新增 4 个端点并入 `nf_api_selftest.py`。

---

### 1.5 方向1 回归与提交切分（1h）

- 跑 `snapshot.py "before_scraps_selftest"` → 再跑 `python scripts/nf_api_selftest.py`（⚠️ 会清空 `data/` 与 `logs/`）。
- **建议 3 个 commit**：① `scrap_cluster.py` + `Temp/test_scrap_cluster.py`；② `stage1 --scraps` + `prompts/stage1_scraps.md` + `fake_client` 分支；③ `ScrapsPanel.vue` + nf_api 4 端点 + `App.vue`。

---

## 2. 方向2：角色关系可视化（约 7h）

### 2.1 依赖（0.5h）

- `console/package.json` dependencies 加 `"d3": "^7.9.0"`（Node 22 / Vite 5 / Vue 3.5.40 兼容）。
- 实际只用 `d3-force` / `d3-drag` / `d3-zoom` / `d3-scale` → 建议**按分包引**（全量 d3 min ≈250KB，会进 electron-builder 打包体积）。
- `package-lock.json` 一并提交。renderer 走 Vite 打包，不需要 CDN。

### 2.2 `console/src/RoleGraph.vue`（4h）

**props**：`{ setting, appearances }` —— 取数放在父级 `App.vue`，组件纯展示，便于复用与排查。

**数据结构映射（基于真实 schema，不是猜的）**

- 节点：`setting.characters[]`，字段 `id / name / role / traits / appearance / relations / experience / source / locked`。
- 节点大小：`appearances.characters[name].total`（出场次数）经 `d3.scaleSqrt` 映射半径。
- 描边：`appearances.characters[name].level` → `major` 实线粗、`minor` 实线细、`background` 灰、`absent` **虚线**（防"写了忘了用"）。
- 角标：`locked: true` 加锁形标（设定集纪律可视化）。
- **边（最大的坑）**：`relations` 是**字符串数组**，形如
  `"夏婉芸: 侍从/徒弟/继承者/眷属/实验对象，月神宠爱万分…"`，
  不是 id 引用。解析流程：
  1. `re.split(r"[:：]", s, 1)` 取左侧为对端名称；
  2. 按 `name` 精确匹配 → 失败则去括号别名再匹配；
  3. 仍匹配不到 → 生成灰色"未入册"幽灵节点（不可拖拽），并在顶部计数提示「N 条关系未匹配到角色，见控制台」。
- 关系类型着色：从描述关键词归类，映射表单独放 `console/src/relationTypes.js`（便于调）：师徒/侍从、亲属、恋慕/伴侣、敌对、同僚/组织、实验体/创造物、其他。无 `relations` 的旧条目只画点不画边。

**交互**：`forceLink/forceManyBody/forceCenter/forceCollide`；拖拽固定、滚轮缩放平移；点节点高亮一阶邻居 + 右栏显示 role/traits/experience。

**性能与主题**：当前 15 个角色（<200），SVG 足够，无需 canvas。IDE 主题为 **light** → 浅底 + 深色文字硬编码，不要依赖未实现的 `c-*` 色类。

### 2.3 数据接入（RoleGraph.vue + App.vue + nf_api，2.5h）

- **后端（原表漏项，F9）**：新增只读端点
  - `GET /setting/appearances` → `{ok, generated_at, total_chapters, characters, new_candidates}`；文件缺失返回 `{ok:false, hint:"先跑 scripts/appearances.py"}`；
  - 可选 `POST /appearances/refresh` → 调 `appearances.count_appearances()` + `write_appearances()` 后回传新数据（纯确定性、零成本、无 LLM，可以放心给按钮）。
- **前端挂载点**：设定页签（`tab === 'story'`）的 `settingTab` 从 4 个扩到 5 个，新增「关系图」（现有：characters / world / plot_fragments / timeline）。放设定页签而非新顶层页签，理由是与 Story Bible 同源。
- 首次进入并行请求 `/setting/current` + `/setting/appearances`；appearances 缺失**不阻塞**，降级为均一节点大小。

### 2.4 验收与风险

- 验收：15 个角色全部成节点；`月神` 的边数 = 其 `relations` 中可匹配项数；absent 角色虚线可见；拖拽/缩放不卡；d3 加载失败时被 catch，其他页签不受影响。
- 风险①：`relations` 自由文本匹配率可能偏低 → 先输出「未匹配名称清单」，再决定是否加别名表 `data/setting/alias.json`（**进 data/，不进 Git**）。
- 风险②：打包体积增长 → 用 d3 分包。

---

## 3. 方向3：质量自评闭环（约 4h，原估 5h）

### 3.1 `scripts/auto_rewrite.py`（新建，1.5h —— 因为复用 F6）

**核心是薄封装，不要重写备份/校验逻辑。**

```python
def collect_targets(progress, threshold=6, only=None) -> list[int]
def build_feedback(chapter_path, quality, check_report=None) -> str
def run_auto_rewrite(cfg, proj, progress, db, cost, run_id=None,
                     threshold=None, max_rounds=1, dry_run=False,
                     client=None, task_dir=None) -> tuple[bool, str, dict]
```

- `collect_targets`：**优先**读 `progress.stages["4"]["needs_rewrite"]`（F4）；**兜底**重扫 `data/chapters/raw/*.md` 的 `<!-- quality: X/10 -->` 取 `< threshold`——因为该列表只在 stage4 当次写入，历史 run 可能没留。
- `build_feedback`：给 `run_single_refine` 用的自然语言意见，可直接用这段模板：
  > 本章 quality 自评 `{q}/10`，低于阈值 `{thr}`。请在**不改变情节与结构**的前提下重写：① 收敛冗余形容与比喻连缀；② 补充具体动作与感官细节；③ 视角统一为客观叙事、删除解释性插入；④ 与大纲及设定 locked 条目保持一致。字数必须落在原字数 ±20% 内。

  若 `data/outline/review_report.json` 已有该章 findings，追加进意见（让重写吃上确定性检查的结论）。
- 落盘：逐章调 `batch_refine.run_single_refine(cfg, proj, n, feedback, ...)`，天然满足 **±20% 铁律 + history 备份 + stage4_refine 提示词**。
- **失败不阻断**：单章失败记录后继续，保留备份以便回退。
- **幂等**：写 `progress.stages["4"]["auto_rewritten"]` 记录已处理章号；`max_rounds=1`（配置 `gates.auto_rewrite_max_rounds`），重写后 `quality` 仍低于阈值则**保留 needs_rewrite 标记**，不再自动重试，转人工。
- **预算保护**：重写前检查 `cost.status(run_id) == "pause"` → 立即中止并记录（别让自动重写把预算打穿）。
- CLI：`python scripts/auto_rewrite.py [--threshold 6] [--chapters 3,7] [--dry-run] [--max-rounds 1]`。

### 3.2 orchestrator 集成（1.5h）

**插入点必须与现有 n==4 钩子排队（F5）。推荐落法 B：**

- 位置：`ok` 为真之后、`if n == 4 and review_after_stage4:` 审稿分支**之前**。
- 开关：`gates.auto_rewrite: false`（**默认关**，与既有 `auto_refine: false` 保持一致的克制姿态）。未获用户确认不开自动改稿——符合模型/成本纪律。
- 日志：`[orchestrator] 检测到 M 章 quality<6，自动重写中（阈值 6，轮次 1）…`
- **冲突规避**：若同时开了 `gates.auto_refine: true`（batch_refine 路径），两套会改同一章 → 启动时检测并打印明确告警，以 auto_rewrite 为先，并提示 batch_refine 只处理审稿报告里的非 quality 类问题。
- 成本归集：复用当前 `run_id` + `charge_cost(run_id, 4, n, result)`，使 `cost_report.py --by-chapter` 能看到重写开销。
- GUI：`/state` 已含 progress，`needs_rewrite` / `auto_rewritten` 可先只读展示；可选加 `POST /auto_rewrite/run`（参数 `threshold` / `dry_run`）。

### 3.3 报告（0.5h）

- `data/outline/auto_rewrite_report.md`，每次运行**追加**（沿用 polish_report 的写法）：

```
## 2026-09-13 14:38 自动重写（阈值 6，轮次 1）
| 章 | 重写前 quality | 重写后 quality | 字数变化 | 备份 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 03 | 4 | 7 | +6.2% | ch03_v2.md | ✅ |
| 07 | 5 | 5 | -1.1% | ch07_v2.md | ⚠️ 仍低于阈值，转人工 |
```

- 末尾附「未通过章号 + 建议人工精修」并指向 `batch_refine` 通道（`data/outline/review_report.json`）。
- 同时写机器可读的 `data/outline/auto_rewrite_report.json`，供 GUI 与统计消费。

### 3.4 验收

- `--dry-run` 只建任务不改文件；
- `NF_API_ALLOW_FAKE=1` 下全链能跑；
- 重写后 `check_chapter` 通过且 `abs(Δ)/before <= 0.2`；
- 连续跑两次 orchestrator **不产生二次重写**（幂等）。

---

## 4. 工期、依赖与里程碑

| 方向 | 子任务 | 工时 | 依赖 |
|------|--------|------|------|
| 1 | 1.1 新建目录 + AGENTS 补说明 | 0.5h | – |
| 1 | 1.2 `scrap_cluster.py` + 单测 | 5h | – |
| 1 | 1.3 stage1 `--scraps` + 提示词 + fake | 5.5h | 1.2 |
| 1 | 1.4 `ScrapsPanel.vue` + nf_api 4 端点 | 7h | 1.2（数据）、1.3（promote 链路） |
| 1 | 1.5 回归 + 提交切分 | 1h | 1.1–1.4 |
| 2 | 2.1 d3 依赖 | 0.5h | – |
| 2 | 2.2 `RoleGraph.vue` + `relationTypes.js` | 4h | 2.1 |
| 2 | 2.3 nf_api 端点 + App.vue 接入 | 2.5h | 2.2 |
| 3 | 3.1 `auto_rewrite.py` | 1.5h | – |
| 3 | 3.2 orchestrator 集成 + 开关 | 1.5h | 3.1 |
| 3 | 3.3 双份报告 | 0.5h | 3.1 |
| 3 | 3.4 验收（dry-run / fake / 幂等） | 0.5h | 3.1–3.3 |
| | **合计** | **约 30h** | |

**关键路径**：`1.2 scrap_cluster` → `1.3 stage1 --scraps`。这是唯一有实质不确定性的环节（碎片命名规律未知）。

**建议的推进节奏**：

1. **半天做 1.1 + 1.2**，拿真实 OB34 碎片跑出 `scraps_index.json` **先给你看聚类结果**，确认口径后再写 1.3 的提示词与分批内联逻辑——避免在错误的聚类假设上写 5 小时。
2. 方向2 是纯前端，与方向1/3 无文件冲突，**可并行**推进。
3. 方向3 最短（4h），建议作为收尾批次，或直接插在方向1 的 1.2 与 1.3 之间的等待期做掉。

**并行冲突面控制**（三方都要改 `App.vue` 与 `nf_api.py`）：
- 方向2 只在 `App.vue` 追加 1 个 `settingTab` sub-tab 块 + 1 个 `<section>`；新文件 `RoleGraph.vue`、`relationTypes.js`。
- 方向1 只在素材页签内追加 1 个 sub-tab 块；新文件 `ScrapsPanel.vue`。
- `nf_api.py` 各自在自己的 `elif` 分支追加，不交叉修改既有行。

---

## 5. 与原始子任务表的差异（需你确认）

| 项 | 原表 | 校准后 | 原因 |
|----|------|--------|------|
| 1.3 stage1 `--scraps` | 4h | 5.5h | 需额外做 F1 指纹修正 + fake_client 分支 + 超限分批 |
| 1.4 GUI | 6h | 7h | 「提炼进卡片」需走 `data/` 中转落盘链路（F2）+ 写前备份 |
| 2.3 数据接入 | 2h | 2.5h | 原表漏了 nf_api 的 appearances 端点（F9） |
| 3.1 auto_rewrite | 3h | 1.5h | 复用了 `batch_refine.run_single_refine`（F6），铁律/备份/提示词全部白拿 |
| 3.2 orchestrator | 1.5h | 1.5h | 不变，但插入点需与现有 n==4 审稿钩子排队（F5） |

---

## 6. 合规红线自查清单

- [ ] 不新增 `.env` 读取；**不修改 `config/system.yaml` 的 `model.*`**（免费包按模型领取，未经确认不换付费模型）。
- [ ] 提示词进 `prompts/stage1_scraps.md`，不硬编码进 `.py`。
- [ ] 模型写入白名单：只允许 `data/**`；`materials/raw/*` 的写入**全部由 Python 完成**。
- [ ] 破坏性操作（碎片删除/卡片覆盖写）→ 先 `snapshot.py` + 备份 + `confirm`。
- [ ] ROSA 设定库零写入（本计划不触碰 `E:/图书馆/ROSA`）。
- [ ] `data/`、`history/`、`output/` 不进 Git；新建的 `materials/original_scraps/` **会进 Git**（若不希望，需显式加 `.gitignore` 条目）。
- [ ] 跑 `nf_api_selftest.py` 前先 `snapshot.py`（会清空 `data/` 与 `logs/`）。

---

## 7. 需你拍板的三个问题

1. **OB34 原始碎片的真实形态**：文件名是否带时间戳？是「前缀_类型.md」还是自由命名？—— 决定 `cluster_key()` 与 `--cluster-by content` 的默认值，建议先跑 1.2 拿真实结果定调。
2. **`gates.auto_rewrite` 默认值**：建议 **false**（显式开启才自动改稿），与现有 `auto_refine: false` 一致。
3. **`materials/original_scraps/` 是否进 Git**：原始碎片若含私人内容，需加 `.gitignore` 条目。

---

# 附录：实现记录（方向1，2026-09-13）

## 已落地清单

| 子任务 | 交付物 | 验证 |
|--------|--------|------|
| 1.1 目录与 Git 隔离 | `materials/original_scraps/`；`.gitignore` 增 `materials/original_scraps/`、`.../_backup/`、`materials/raw/_backup/`；`AGENTS.md` 补关键路径 / 硬性约束 / 命令速查 | `git check-ignore` 通过 |
| 1.2 碎片聚类 | `scripts/utils/scrap_cluster.py` | `Temp/test_scrap_cluster.py` **44 断言全过** |
| 1.3 stage1 接入 | `stage1_consolidate.py`（`--scraps/--with-scraps/--no-scraps`）、`prompts/stage1_scraps.md`（新）、`prompts/stage1_materials.md`（加 `{{extra_inputs}}`）、`utils/fake_client.py`（碎分支）、`config/project.yaml`（新增 `materials:` 段） | `Temp/test_stage1_scraps.py` **30 断言全过** |
| 1.4 GUI + 端点 | `console/src/ScrapsPanel.vue`（新）、`scripts/nf_api.py`（`/scraps/list·read·save·delete·promote`）、`App.vue` 素材页签子页签 | `Temp/test_scraps_api_http.py` **29 断言全过**；`npm run build` 通过 |
| 1.5 回归 | `nf_api_selftest.py` 增碎片只读端点用例 | 见下方"未跑项" |

## 实际接口（与计划草稿的差异）

- 模块 API 定为：`scan_scraps()` / `score_keywords()` / `build_edges()` / `cluster_scraps()`
  / `build_index()` / `write_index()` / `content_fingerprint()` / `build_inline_batches()`，
  顶层便捷入口 `collect(dir_) -> (index, warnings)`。
- stage1 侧：`prepare_scraps()`（原计划叫 `load_scraps`）、`build_scraps_task()`、
  `validate_scraps_merge()`、`merge_scraps_batches()`、`run_scraps_merge()`。
- 指纹签名：`manifest_fingerprint(manifest, extra_fingerprints=())` —— 传碎片的
  **content_fingerprint**，不传 index 文件字节（后者含 `generated_at`，会导致每次都误判有变化）。

## 计划外的必要增补

1. **否定语境剔除**：碎片里"这和月神无关"会把 `月神` 的 df 抬高、把真簇拆散 → 加
   `negated_tokens()`，并在产物里透明记录 `negated` 供人工复核。
2. **纯数字日期文件名**：`0312` / `20250406` 这类自由命名很常见 → 增加整名匹配（仅当文件名
   **整体**是数字串时生效，`第0312条草稿.md` 不会被误判）。
3. **极大 n-gram 归并**：无分词器时"月兔密室"会派生出 `兔密室/月兔密/兔密/密室` 占满关键词位
   → 加 `_select_keywords()`；同时把**匹配用候选集**与**展示用关键词**分离（否则 `月兔` 被归并掉、
   跨文档匹配不上）。
4. **标点即分隔符**：`normalize_text` 原本把标点整个删掉，导致 n-gram 跨句（"同类。待定" →
   `同类待定` 污染簇名）→ 改为替换为空格。
5. **碎片-only 场景**：`materials/raw` 为空但有碎片时，不再直接判失败，改为仅用碎片归并。
6. **修掉一个既有 bug**：`nf_api` 的 `/materials/remerge` 注释写着"清 fingerprint"但实际没清，
   导致素材无变化时按钮静默"复用设定集"、名不副实 → 已补上真实清指纹。

## 有意推迟

- **碎片→卡片的「AI 提炼」通道（原计划 1.4 的一半）**：暂不实现。理由：每次点击都产生 LLM 费用，
  而 stage1 归并阶段本就会用 LLM 消化碎片（`scraps_merge.json` 已喂进归并任务），
  在 GUI 再调一次属于重复付费；且本地拼卡零成本、结果可预期。
  若仍需 AI 通道：新增 `prompts/scrap_promote.md` + `fake_client` 分支 + 一个 `mode:"ai"` 参数即可。

## 未跑项（需用户确认后才可执行）

- `scripts/nf_api_selftest.py` **会清空 `data/` 与 `logs/`**，而当前仓库里有《留下你的歌》的
  真实产物（setting.json、chapters/raw/*.md）。加 `--force` 才能跑，等于销毁当前书档
  （快照可回退）。**未经确认不执行**。碎片端点的写入类覆盖已由
  `Temp/test_scraps_api_http.py` 在临时项目根内完成，不受此限制。

